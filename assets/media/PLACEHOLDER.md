# Demo Media Placeholders

Put your real demo files here with these exact names:

- `town-demo-1.jpg` (or png/webp, then update `index.html`)
- `town-demo-2.jpg` (or png/webp, then update `index.html`)
- `town-demo.mp4`

The website already contains placeholder blocks. Replace them with your files and update the paths if needed.
